import './ForecastTile.css'

const ForecastTile = ({forecast}) => {
  return (
    <div>
        <span>{forecast}</span>
        <span>{forecast}</span>
        <span>{forecast}</span>
        <span>{forecast}</span>
    </div>
  )
}

export default ForecastTile